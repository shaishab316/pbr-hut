# OTP module

This page is referenced from [Auth module documentation](/auth/auth). Expand it with:

- How `OtpService` generates and verifies TOTPs (otplib)
- Nonce and Redis TTL alignment with `AuthCacheRepository`
- Environment variables (`OTP_SECRET`, optional `TEST_OTP`)

**Source:** `src/modules/otp/`
